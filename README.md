
  # CRM para Call Center

  This is a code bundle for CRM para Call Center. The original project is available at https://www.figma.com/design/SJT40SmGE4gB6GfE0RTrv6/CRM-para-Call-Center.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  